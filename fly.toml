app = "nexus"
primary_region = "bom"
[build]
  dockerfile = "Dockerfile"
[env]
  NODE_ENV = "development"
  PORT = "8080"
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  min_machines_running = 0
[[http_service.checks]]
  path = "/health"
  interval = "30s"
  timeout = "5s"
